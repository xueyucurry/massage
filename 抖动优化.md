# FAIRINO 按摩运动抖动优化与复现指南

## 1. 文档目的

本文记录 2026-08-14 已在实际机械臂上确认“完全不抖动”的 FAIRINO 按摩运动方案。后续 Codex 或维护者遇到抖动、顿挫、点位堆积、接触瞬间冲击等问题时，应先按本文确认控制链路和运行版本，再修改参数或代码。

稳定基线就是“包含本文的提交”。主要实现位于：

- `robots/fairino/ft.py`
- `robots/fairino/fairino_ros2/frcobot_ros2-master/fairino_hardware/include/fairino_hardware/command_server.hpp`
- `robots/fairino/fairino_ros2/frcobot_ros2-master/fairino_hardware/src/command_server.cpp`
- `robots/fairino/run_lasttime_ros2.sh`
- 根目录 `massage`
- `robots/fairino/tests/test_motion_interpolation.py`

现场结论：当前参数和实现组合已经由用户确认不再抖动。后续优化应采用“小改动、单变量、可回退”的方式，不能把多个控制层同时改掉。

## 2. 先理解真正的根因

本次抖动不是一个参数造成的，而是多个控制时序问题叠加。

### 2.1 旧完成标志被误认为本次运动已完成

原逻辑发送 `MoveL` / `MoveJ` 后，只要状态话题里看到 `motion_done=1` 就继续。控制器可能在新命令刚被接受时仍发布上一段运动遗留的 `motion_done=1`，于是上层会在机械臂尚未到位时继续发送后续目标。

结果是：

- 多个短目标在控制器中堆积；
- 上层的读力、下一步位移和机械臂真实位置不同步；
- 运动表现为抖动、急停急启、突然补走或相邻点来回修正。

当前修复位于 `Ros2RobotProxy.wait_motion_done()`：

1. 命令完成后只接受序号更新的状态帧；
2. 同时校验实际 TCP 位姿或实际关节角已经接近本次目标；
3. 连续多个新状态帧都满足条件才算完成；
4. 短距离运动采用与本次位移成比例的更小容差，避免 0.1 mm 的命令被 3 mm 固定容差直接判为到位；
5. 欧拉角差值按最短角距离计算，避免 `179°` 与 `-179°` 被误判为相差 `358°`。

不可退化为“只看 `motion_done`”或“只 sleep 固定时间”。这是防止命令堆积的第一道约束。

### 2.2 短距离 `MoveCart` 反复启停

旧力控流程会把相邻的微小位移拆成许多独立 `MoveCart`。每一步都有启动、加速、减速和停止，相邻步骤之间还夹有 ROS service 往返与 Python 调度延迟。即使每个目标都正确，机械臂也会表现为一顿一顿。

当前新增控制服务命令 `ServoCartSmooth`：

- 在 C++ ROS 2 控制服务内部开启一次 `ServoMoveStart`；
- 默认以 125 Hz，即 8 ms 周期连续发送 `ServoCart`；
- 使用五次 smoothstep 插值：

```text
u = t / T
s(u) = 10u^3 - 15u^4 + 6u^5
pose(t) = start + s(u) * (target - start)
```

该曲线两端的速度和加速度均为 0，避免阶跃。插值时长按五次曲线的峰值速度倍率 `1.875` 计算，并保证至少 8 个控制周期。结束前重复发送 3 次精确终点，让控制器消费完最后采样后再退出伺服模式。

Python 侧 `MoveCartInterpolated()` 仅把短距离、较小姿态变化、`tool=0`、`user=0` 的运动交给该命令；长距离安全转场仍使用原 `MoveCart`。高频插值必须留在控制服务内，不能改成 Python 每 8 ms 调一次 ROS service。

### 2.3 力贴近被拆成“读力—走一步—停住”的循环

旧 `_approach_to_target_force()` 在 Python 中循环读力并发送离散位置目标。接触前后步长与速度切换会带来明显的启停和冲击，Python 与 ROS service 的非确定性延迟还会破坏周期。

当前新增 `ServoCartForceApproach`，整个贴近过程只使用一次 `ServoMoveStart` / `ServoMoveEnd` 会话：

1. 每个 8 ms 周期读取六维力；
2. 根据未接触、已接触、接近目标和非常接近目标四个阶段选择速度；
3. 使用加速度和减速度上限平滑切换速度；
4. EMA 滤波力只用于选择速度档位；
5. 达标计数、安全限位仍使用原始力，不能用滤波结果掩盖尖峰；
6. 达到目标力需要连续原始采样确认；
7. 正常结束前保持最后目标 3 个周期；
8. 到达搜索终点、超时、读力失败或安全超限后明确结束伺服。

安全错误绝不回退为普通 `MoveCart`。在已经可能接触人体时，力感知控制失败后继续位置运动可能造成过压。

### 2.4 运动方向和测力坐标系曾不一致

`FT_GetForceTorqueRCS(1)` 使用活动工具坐标系的 Fz。保持当前 TCP 姿态时，如果位置偏移仍沿视觉局部法向，而不是当前姿态对应的真实工具 Z 轴，机械臂的“压入方向”和用于判定的 Fz 就不是同一物理方向，容易发生横向修正和力值异常。

当前 `_pose_from_frame_offset()` 在保持姿态模式下：

- 用 `motion_orientation` 计算真实工具 Z 轴；
- 沿该轴生成贴近位姿；
- 把分筋方向投影到工具平面；
- 保证位移方向、工具姿态和 Fz 判定语义一致。

启动脚本现在以 `FT_KEEP_CURRENT_ORIENTATION` 为主，同时兼容旧变量 `LASTTIME_FORCE_KEEP_CURRENT_ORIENTATION`。不要再出现“脚本导出了旧变量，但 Python 根本没有读取”的情况。

### 2.5 源码已更新，但运行的仍是旧控制服务

只重新编译不会自动替换已经运行的 `ros2_cmd_server` 进程。若 Python 已使用新命令，而后台仍是旧二进制，会得到未知命令、意外回退或看似参数无效。

根目录 `massage` 和 `run_lasttime_ros2.sh` 会比较：

```text
安装二进制的 device:inode
/proc/<pid>/exe 的 device:inode
```

若不是同一个已安装二进制，并且没有按摩流程正在运行，则重启服务加载新版本；若另一个 `ft.py` 正在运行，则拒绝中途切换。排查抖动时必须先确认实际运行的二进制，不要只检查源码。

## 3. 当前控制链路

```text
视觉轨迹/按摩动作
        |
        v
ft.py 生成目标位姿、接触轴和力目标
        |
        +-- 长距离转场 ----------> 单次原生 MoveL / 连续分段兜底
        |                           直达失败才生成平滑衔接中间点
        |                           新状态 + 实际到位 + 连续稳定帧
        |
        +-- 短距离力控动作 ------> ServoCartSmooth
        |                           125 Hz 五次插值
        |
        +-- 从悬空位力贴近 ------> ServoCartForceApproach
                                    125 Hz 读力、连续调速、安全停机
```

`ft.py` 负责动作语义和安全策略，C++ 控制服务负责需要固定周期的实时性更强的伺服流。不要把 125 Hz 内环搬回 Python。

长距离安全转场仍保持“先抬升、再高位平移、最后下降”的路径。每条直线优先使用
一次原生 MoveL 连续直达，避免每 50 mm 人为减速到零并等待一次 ROS service 往返。
只有直达失败并停止运动后，才按 `ROS2_SEGMENT_MAX_STEP_MM` 生成共线兜底点；中间点
以非阻塞 MoveL 加入控制器平滑队列，只在最终目标严格等待到位。抬升、高位平移和
下降之间仍保留必要的安全边界验收。

## 4. 当前稳定默认值

| 参数 | 当前默认值 | 用途 |
| --- | ---: | --- |
| `FT_SERVO_INTERPOLATION` | `1` | 启用短距离五次插值 |
| `FT_SERVO_INTERPOLATION_HZ` | `125` | 与 8 ms 伺服周期对齐 |
| `FT_SERVO_MIN_SAMPLES` | `8` | 短动作最少控制采样 |
| `FT_SERVO_MAX_DISTANCE_MM` | `30` | 超过后使用普通转场 |
| `FT_SERVO_MAX_ORIENTATION_DEG` | `15` | 超过后使用普通转场 |
| `ROS2_MOTION_DONE_STABLE_SAMPLES` | `2` | 到位连续稳定帧数 |
| `ROS2_MOTION_DONE_TOL_FRACTION` | `0.25` | 小步动态到位容差比例 |
| `FT_CONTINUOUS_FORCE_APPROACH` | `1` | 启用连续力贴近 |
| `FT_CONTINUOUS_FORCE_APPROACH_HZ` | `125` | 连续贴近周期 |
| `FT_CONTINUOUS_FORCE_COARSE_SPEED_MM_S` | `18` | 未接触速度 |
| `FT_CONTINUOUS_FORCE_CONTACT_SPEED_MM_S` | `4` | 接触后速度 |
| `FT_CONTINUOUS_FORCE_FINE_SPEED_MM_S` | `4` | 接近目标速度 |
| `FT_CONTINUOUS_FORCE_NEAR_SPEED_MM_S` | `3` | 临近目标速度 |
| `FT_CONTINUOUS_FORCE_ACCEL_MM_S2` | `120` | 加速度上限 |
| `FT_CONTINUOUS_FORCE_DECEL_MM_S2` | `240` | 减速度上限 |
| `FT_CONTINUOUS_FORCE_FILTER_ALPHA` | `0.35` | 仅用于调速的 EMA 系数 |
| `FT_CONTINUOUS_FORCE_TARGET_STABLE_SAMPLES` | `2` | 原始力连续达标次数 |
| `FT_CONTINUOUS_FORCE_TIMEOUT_S` | `20` | 单次贴近超时 |
| `LASTTIME_FORCE_APPROACH_MAX_OFFSET_MM` | `35` | 背部最大接触搜索距离 |
| `THIGH_FORCE_APPROACH_MAX_OFFSET_MM` | `35` | 大腿最大接触搜索距离 |
| `FT_KEEP_CURRENT_ORIENTATION` | 启动脚本默认 `1` | 保持姿态并沿真实工具 Z 轴贴近 |

这些值是已验证组合，不代表所有床垫、工具或目标力都适合。更换末端、力传感器、控制器周期或人体支撑材料后，应重新做低力、低速验证。

## 5. Codex 排查顺序

后续收到“又开始抖了”的反馈时，按以下顺序检查，不要先盲目降速或加 sleep。

### 第一步：确认现象边界

至少区分：

- 悬空运动是否抖；
- 只有接触瞬间抖，还是保压期间也抖；
- 只在点筋/分筋发生，还是长距离转场也发生；
- 抖动是高频小幅振荡，还是低频的走走停停；
- 是否在更改工具、姿态、目标力、轨迹或重编译后首次出现。

这几类现象对应不同层级，不能混为一个“速度参数问题”。

### 第二步：确认运行版本

1. 检查当前分支、提交和工作树；
2. 确认控制服务已重新构建；
3. 确认运行进程 `/proc/<pid>/exe` 与安装目录二进制身份一致；
4. 检查启动输出是否包含：
   - `力控短距离运动: 125Hz 五次插值`
   - `力控贴近: 125Hz 连续伺服`
   - `贴近姿态: 保持当前 TCP 姿态并沿工具 Z 轴贴近`
5. 若报告不支持 `ServoCartSmooth` 或连续伺服命令，先修复构建/重启，不要调参数。

### 第三步：判断是哪条控制路径

- 长转场抖：优先检查完成判定、状态话题新鲜度、目标堆积和 MoveL/MoveJ；
- 短距离悬空动作抖：检查 `ServoCartSmooth` 是否实际启用、周期和时长；
- 力贴近抖：检查 `ServoCartForceApproach`、原始 Fz、速度档位和加减速；
- 保压抖：检查 `_hold_target_force()` 的 `KP`、最大单步和测力噪声，不要误改贴近内环；
- 横向摆动或 Fz 异常：检查工具坐标系、当前姿态、工具 Z 轴和局部法向是否一致。

### 第四步：只改一个变量

推荐顺序：

1. 先保证新状态与真实目标到位判定正确；
2. 再保证 125 Hz 周期连续且没有 service 往返插入内环；
3. 接触冲击大时先降低 `COARSE_SPEED` / `CONTACT_SPEED`，或降低加速度；
4. 临近目标过冲时再降低 `NEAR_SPEED`、提高减速度或增加稳定采样数；
5. 只有确认力噪声影响速度档位时才调整 `FILTER_ALPHA`；
6. 保压振荡才调整 `LASTTIME_FORCE_HOLD_KP_MM_PER_N` 和 `LASTTIME_FORCE_HOLD_MAX_STEP_MM`。

每次只改一项并记录现场结果。一次同时改频率、速度、滤波和 KP，会失去因果证据。

## 6. 不可破坏的安全约束

后续修改必须保留：

1. 连续贴近的法向力、切向力和力矩软件限位；
2. `FT_GetForceTorqueRCS` 读力失败立即停止；
3. 连续贴近超时立即停止；
4. 安全错误不回退普通位置运动；
5. 目标力达标和安全判断使用原始力，EMA 只用于调速；
6. 伺服目标平移和姿态变化的上限检查；
7. 运行中的按摩流程不切换控制服务二进制；
8. 更换坐标系或姿态时，Fz 定义和运动接触轴必须同步审查；
9. 最大接触搜索距离默认保持 35 mm，不能无证据恢复到 150 mm；
10. 硬件复测从低目标力、人体外替代物和急停可达状态开始。

## 7. 验证方法

### 7.1 无硬件测试

从仓库根目录执行：

```bash
source /opt/ros/humble/setup.bash
source robots/fairino/fairino_ros2/frcobot_ros2-master/install/setup.bash
PYTHONPATH="robots/fairino:${PYTHONPATH:-}" env/.venv/bin/python -m unittest discover \
  -s robots/fairino/tests -p 'test_*.py'
python3 -m py_compile robots/fairino/ft.py
bash -n massage
bash -n robots/fairino/run_lasttime_ros2.sh
```

单元测试覆盖：

- 旧 `motion_done` 不能让错误目标提前完成；
- 欧拉角跨 `±180°` 的最短角差；
- 短动作容差必须小于动作本身；
- 五次插值最少采样和峰值速度时长；
- 连续贴近结构化返回；
- 连续贴近必须携带全部安全参数；
- 安全失败不能回退位置运动；
- 固定姿态时沿真实工具 Z 轴生成 offset。

C++ 变更还应在 ROS 2 工作空间构建：

```bash
cd robots/fairino/fairino_ros2/frcobot_ros2-master
colcon build --packages-select fairino_hardware
```

### 7.2 硬件验证

1. 确保急停可立即触达，先在无人体状态检查短距离悬空运动；
2. 使用低目标力和可压缩替代物测试接触；
3. 观察是否出现命令堆积、停走交替或接触过冲；
4. 验证法向力、切向力、力矩超限和超时能停止；
5. 分别验证长转场、短插值、连续贴近和保压；
6. 最后再恢复正式目标力和完整轨迹。

截至本文稳定基线，用户已完成现场体验并确认机械臂完全不抖动。自动化测试只能验证时序和安全逻辑，不能替代这项硬件结论。

## 8. 临时隔离与回退

为定位问题可临时关闭单个新路径：

```bash
FT_SERVO_INTERPOLATION=0                 # 短距离动作退回 MoveCart
FT_CONTINUOUS_FORCE_APPROACH=0            # 力贴近退回旧分步流程
FT_KEEP_CURRENT_ORIENTATION=0             # 使用局部深度平面法向
```

这些开关只用于 A/B 定位，不代表推荐运行配置。特别是关闭连续力贴近后，旧分步流程本身可能重新出现启停顿挫。任何回退都不得绕过软件力限位或扩大接触搜索范围。

若新问题无法快速定位，应回到包含本文的稳定提交复测，再把后续提交逐个引入；不要复制粘贴某一段旧控制逻辑与新逻辑混用。

## 9. Review 清单

Codex review 抖动相关改动时逐项回答：

- [ ] 是否可能把上一段运动的状态当成本次完成？
- [ ] 是否同时验证了更新后的状态帧和真实目标到位？
- [ ] 高频伺服循环是否完全位于 C++ 控制服务内？
- [ ] 插值两端的速度、加速度是否连续？
- [ ] 是否给控制器留出消费最后目标采样的时间？
- [ ] 力贴近是否保持一次连续伺服会话？
- [ ] 速度切换是否有限制加速度和减速度？
- [ ] 原始力是否仍用于达标与安全判断？
- [ ] 读力失败、超时、超限是否立即停止且不回退位置运动？
- [ ] 运动接触轴是否与工具坐标系 Fz 一致？
- [ ] 实际运行的控制服务是否就是刚构建的二进制？
- [ ] 是否保留了单元测试，并完成低风险硬件复测？

只有这些问题都能用代码、日志或现场测试回答，才能认为抖动修复可合入。
